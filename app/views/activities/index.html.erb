<div class="container">
  <h1>Liste des activités</h1>
  <ul>
    <% @activities.each do |activity| %>
      <li>
        <%= activity.title %>
      </li>
    <% end %>
  </ul>
  <%= link_to "Retour vers la page d'accueil", root_path, class: "btn btn-light" %>
</div>
