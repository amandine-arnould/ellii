<div class="container">
  <div
    class="twilio-video"
    data-sid="<%= ENV["ACCOUNT_SID"] %>"
    data-session="<%= @booking.url_session %>"
    data-token="<%= @token %>"
    >
    <div class="row justify-content-between">
      <div class="col-7">
        <div class="participant">
        </div>
      </div>
      <div class="col-4">
        <div class="remote-media-div text-center">
          <video id="userVideo" autoplay muted="true" ></video>
          <div>
            <button id='leave-btn' class='btn btn-primary mt-3'>
              Quittez
            </button>
          </div>
        </div>
      </div>
    </div>
    <%# <div class="row">
      <div class="col-7">
        <p class="text-center"> %>
          <%#= @booking.booker == current_user ? @booking.proposal.proposer.email : @booking.booker.email %>
        <%# </p>
      </div>
    </div> %>
    <%#= link_to '', root_path, class: 'd-none root-link', data: {turbolinks: false}  %>
  </div>
</div>
